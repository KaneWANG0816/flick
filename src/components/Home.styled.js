import styled from "styled-components";

const StyledHome = styled.div`
    margin: 10;
    height: 100vh;
`

const StyledFilter = styled.div`
    text-align: right;
`

const StyledWelcome = styled.div`
    text-align: center;

    .welcome-header{

    }

    .welcome-message{

    }
`

const StyledGo = styled.div`
    text-align: center;

    .go-name{

    }

    .btn-go{

    }
`

const StyledStatus = styled.div`
    text-align: center;

    .status-dot{

    }
`

const StyledFooter = styled.div`
    text-align: center;
    position: absolute;
    width: 100%;
    bottom: 0;
    margin: 0 auto;

    .disclaim{
        
    }
`

const StyledLoad = styled.div`

`

export {StyledFooter, StyledFilter, StyledGo, StyledLoad, StyledStatus, StyledWelcome, StyledHome}